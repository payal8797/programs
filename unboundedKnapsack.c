#include<stdio.h>
#include<stdlib.h>

int unboundedKnapsack(int k, int arr_count, int* arr)
{
    
}


int main()
{
    int i,k,arr_count,arr[100];
    printf("Enter number of elements in array:\n");
    scanf("%d",&arr_count);
    for(i=0;i<arr_count;i++)
    {
        scanf("%d",&arr[i]);
    }
    printf("Enter target sum:");
    scanf("%d",&k);
    int unboundedKnapsack(k,arr_count,arr);
}

