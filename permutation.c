#include<stdio.h>
#include<stdlib.h>

int main()
{
    int n,i,j;
    char s[10];
    printf("Enter number of characters to be permuted:");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%s",s);
    }
    
}